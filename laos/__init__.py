"""LAOS core — Laurent Agent Operating System runtime.

LAOS is the orchestration layer. This package materializes the
orchestration loop (resolve needs -> WDL preflight -> dispatch ->
validate -> verify -> push) as code, with durable state, observability,
and a portfolio control plane.
"""

__version__ = "0.2.0"
