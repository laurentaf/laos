# Spec — `<feature-name>`

> **Stub-por-design.** This is template `_template.md` mirrored from
> `registry/spec-templates/spec/specs/000-bootstrap/spec.md`. **Canonical
> ownership**: LAOS-global registry. Local edits in this meta-project
> violate §6 (Imutabilidade) of `knowledge/sdd-principles.md`.
>
> For the lacareerops-refactor meta-project, the bootstrap spec is
> implicit in:
> - `contract.md` — the project promise
> - LACOUNCIL proposal `ba9a9bd7-...` — the architecture decision
> - ADR-013 (canonical) — the implementation blueprint
>
> Use this file as the structural placeholder until/unless the
> meta-project spawns a real sub-spec (e.g., for a Stage 7 truth-table).

## Contexto

> Estado real antes desta spec. Que dor existe hoje que justifica
> esta spec? Cite números/fatos.

## Decisão inicial

> O que decidimos AGORA (não a decisão final — o que muda depois).
> Use verbos no indicativo.

## Critérios de pronto

> Como saber se a spec foi entregue com sucesso. 3-5 bullets,
> testáveis, mensuráveis.

## Consequências

> O que esta decisão implica (compras, contratempos, alternativas
> descartadas).

## Notas de implementação

> Detalhes técnicos: APIs, libs, ferramentas, comandos. Use como
> runbook.
